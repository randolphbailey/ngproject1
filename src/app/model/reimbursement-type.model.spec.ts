import { ReimbursementType } from './reimbursement-type.model';

describe('ReimbursementType', () => {
  it('should create an instance', () => {
    expect(new ReimbursementType()).toBeTruthy();
  });
});
