import { ReimbursementStatus } from './reimbursement-status.model';

describe('ReimbursementStatus', () => {
  it('should create an instance', () => {
    expect(new ReimbursementStatus()).toBeTruthy();
  });
});
